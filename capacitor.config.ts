import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.homeapp.homeapp',
  appName: 'homeapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
